#include<stdio.h>

int main(){
    if(1)
    {
        printf("This integer is work with if condition\n");
    }
    if(67327)
    {
        printf("These integers is work with if condition\n");
    }
    if(3.14)
    {
        printf("These float (point value)is work with if condition\n");
    }
    if('#')
    {
        printf("These character is work with if condition\n");
    }
    return 0;
}

//if(0){} if '0' condition can't be work 