#include <stdio.h>

int main()
{
    int age;
    printf("Enter any number :");
    scanf("%d", &age);
    if (age > 10)
    {
        printf("a is greater than 10");
    }
    return 0;
}