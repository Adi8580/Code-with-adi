#include<stdio.h>
// How to find the area of cube
int main(){
    int a;
    printf("Put the value of Cube :");
    scanf("%d", &a);
    // If we need to multiply the value write ( a*a*a )
    printf("The Volume of cube is :%d",a*a*a);
    /*
    If we want to find the Volume of cube 
    we need to multiply the value is three time
    */
    return 0;
}