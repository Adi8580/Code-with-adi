#include <stdio.h>
int main()
{
    float FLOAT;
    /*
    float is use to store fractional values
    FLOAT is a variable declared to store a floating point number
    */
    FLOAT = 3.14000;

    printf("The float value is %f", FLOAT);
    return 0;
}