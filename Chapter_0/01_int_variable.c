#include<stdio.h>
int main()
{
    int age;
    /*
    int is a integer for storing whole numbers and age is variable 
    age is declarated to store the age of the user
     */  

    age=100;

    printf("The age of person is :%d",age);
    return 0;
}