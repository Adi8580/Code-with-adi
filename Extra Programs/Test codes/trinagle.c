#include<stdio.h>

int main(){
    int b = 8;
    int h = 37;
    int area = (b * h) / 2;
    printf("The area of the triangle is: %d\n", area);
    return 0;
}
//This thing is use for find area of triangle with the help of (B*H/2) formula