#include<stdio.h>

int main(){
    float a=9.0;
    int b = 2;
    float c = a / b;
    printf("The value of a or b is %.1f",c);
    return 0;
}
// if we take integer int then the value of a and b is 4 
// but we take float variable then we get the value of a and b is 4.5 